# CS2 Helper Bot

Telegram-бот для помощи игрокам в Counter-Strike 2: раскидки гранат, настройки, мета-информация, тренировки, словарь терминов и тактики.

## 📋 Функционал

- **🗺️ Раскидки гранат** – поиск и просмотр позиций для смоков, флешек, молотовых и гранат на всех актуальных картах.
- **⚙️ Настройки** – параметры запуска, оптимизация под разные ПК, прицелы, бинды и конфиги профессиональных игроков.
- **📊 Мета-гейм** – тир-листы оружия, статистика, экономика, пул карт.
- **🎯 Тренировки** – паттерны спрея и лучшие карты для практики из мастерской Steam.
- **📚 Словарь терминов** – объяснение игровых терминов и сленга.
- **🎮 Тактика** – командные стратегии для популярных карт.
- **⭐ Избранное** – сохранение любимых раскидок.
- **🔍 Поиск** – интеллектуальный поиск по гранатам и терминам (включая inline-режим).

## 🛠 Требования

- Python 3.9+
- pip и venv (рекомендуется)
- Токен бота Telegram (получить у [@BotFather](https://t.me/BotFather))
- (опционально) Права администратора для некоторых команд

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/cs2-helper-bot.git
cd cs2-helper-bot
```

### 2. Создание виртуального окружения (рекомендуется)
Windows (PowerShell):

```powershell
python -m venv venv
.\venv\Scripts\Activate
```
Если возникает ошибка выполнения сценариев, выполните в PowerShell от имени администратора:

```powershell
Set-ExecutionPolicy RemoteSigned
```
Подтвердите выбор ```Y``` или ```A```.

### Linux / macOS:

```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Обновление pip и установка зависимостей
```bash
python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

### 4. Настройка переменных окружения
Создайте файл ```.env``` в корне проекта (рядом с ```bot.py```) и заполните:

```env
BOT_TOKEN=ваш_токен_бота
ADMIN_ID=ваш_telegram_id  # можно несколько через запятую: 123456,789012
```
Получить свой ```ADMIN_ID``` можно у бота [@LeadConverterToolkitBot](https://t.me/LeadConverterToolkitBot)

### 5. Подготовка медиа-файлов
Структура папок будет создана автоматически при первом запуске.
Для корректной работы рекомендуется добавить изображения прицелов, паттернов спрея и т.д. в соответствующие подпапки ```media/``` согласно именам, используемым в коде (например, ```media/crosshairs/simple.png```, ```media/sprays/ak47_spray.gif``` и т.д.).

### 6. Запуск бота
```bash
python bot.py
```
Для остановки – ```Ctrl+C```

### ⚠️ Возможные проблемы и их решения
### Импорты не находятся (ModuleNotFoundError)
Убедитесь, что все зависимости установлены и виртуальное окружение активировано.
Переустановите зависимости:

```bash
pip install --upgrade -r requirements.txt
```

### Ошибка при создании виртуального окружения в Windows
Если команда ```python -m venv venv``` не работает, попробуйте:

```powershell
py -m venv venv
```

### Политика выполнения скриптов в Windows
При активации окружения может появиться ошибка о запрете выполнения сценариев.
Выполните в PowerShell (администратор):

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### База данных не создаётся
Проверьте права на запись в папку ```data/```. Если папка отсутствует, создайте её вручную:

```bash
mkdir data
```

### Не загружаются медиа-файлы (изображения, гифки)
Убедитесь, что файлы находятся в правильных подпапках ```media/``` и названы точно так, как ожидается в коде (например, ```media/nades/nade_1_position.jpg```). При необходимости можно временно отключить отправку медиа, чтобы проверить текстовую часть.

### 📁 Структура проекта
```
cs2-helper-bot/
├── bot.py                 # Главный файл запуска
├── config.py              # Конфигурация и пути
├── database.py            # Работа с SQLite
├── keyboards.py           # Inline и reply клавиатуры
├── handlers/              # Обработчики команд (админ, настройки и т.д.)
│   ├── __init__.py
│   ├── admin.py
│   ├── favorites.py
│   ├── meta.py
│   ├── nades.py
│   ├── search.py
│   ├── settings.py
│   ├── tactics.py
│   ├── terms.py
│   └── training.py
├── utils.py               # Вспомогательные функции
├── requirements.txt       # Зависимости Python
├── .env                   # Переменные окружения (не в репозитории)
├── .gitignore
├── README.md
├── data/                  # База данных SQLite (создаётся автоматически)
└── media/                 # Медиа-файлы (изображения, гифки, конфиги)
    ├── crosshairs/
    ├── nades/
    ├── sprays/
    └── configs/
```

### 🧰 Команды бота
```/start``` – приветствие и главное меню

```/help``` – справка и контакты поддержки

```/admin``` – панель администратора (только для указанных в ADMIN_IDS)

```/search <запрос>``` – быстрый поиск (можно просто написать запрос в inline-режиме: @cs2helperobot смок мираж)

### 💬 Поддержка
Если у вас возникли вопросы или предложения, создайте [issue](https://github.com/adreszanyat/cs2helperobot/issues/new) или напишите разработчику в Telegram: [@techsupportuser](https://t.me/techsupportuser)


---

## 📄 .gitignore

```gitignore
# Виртуальное окружение
venv/
.env/

# База данных и медиа
data/
media/

# Кэш Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Файлы окружения
.env
*.log

# IDE
.vscode/
.idea/

# Системные файлы
Thumbs.db
.DS_Store
```


